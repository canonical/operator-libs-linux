[tox]
envlist = py3-{test,lint}
skipsdist = True

[testenv]
deps =
  lint: black
  test: coverage
  test: pyfakefs
  test: pytest
commands = 
   lint: black --line-length 99 --diff --check lib tests
   test: coverage run --source=lib -m pytest 
   test: coverage report -m
